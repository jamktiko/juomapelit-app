import { TestBed } from '@angular/core/testing';

import { LobbycodeService } from './lobbycode.service';

describe('LobbycodeService', () => {
  let service: LobbycodeService;

  beforeEach(() => {
    TestBed.configureTestingModule({});
    service = TestBed.inject(LobbycodeService);
  });

  /*it('should be created', () => {
    expect(service).toBeTruthy();
  });*/
});
